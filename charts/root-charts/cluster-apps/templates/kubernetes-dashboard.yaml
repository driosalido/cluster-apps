{{- if and (.Values.kubernetesDashboard) (.Values.kubernetesDashboard.enable) -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubernetes-dashboard-{{ .Values.envTag }}
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.argocd.project }}
  source:
    repoURL: {{ .Values.repoUrl }}
    path: charts/apps-charts/kubernetes-dashboard
    targetRevision: {{ .Values.targetRevision }}
    helm:
      values: |
        kubernetes-dashboard:
        {{- toYaml .Values.kubernetesDashboard | nindent 10 }}
      parameters:
      - name: kubernetes-dashboard.serviceAccount.name
        value: {{ .Values.kubernetesDashboard.serviceAccountName }}
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system-{{ .Values.envTag }}
  syncPolicy:
    automated:
      prune: true
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 1m
{{- end -}}
